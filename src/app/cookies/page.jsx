import CookiePolicyPage, { metadata as pageMetadata } from "../cookie-policy/page";

export const metadata = {
    ...pageMetadata,
    alternates: {
        canonical: "/cookie-policy",
    },
};

export default CookiePolicyPage;
