import PrivacyPolicyPage, { metadata as pageMetadata } from "../privacy-policy/page";

export const metadata = {
    ...pageMetadata,
    alternates: {
        canonical: "/privacy-policy",
    },
};

export default PrivacyPolicyPage;
